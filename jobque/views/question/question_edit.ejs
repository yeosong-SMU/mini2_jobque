<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>질문 수정</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/header', { user: session.userid }) %>

  <main class="edit-question">
    <h2>질문 수정</h2>

    <form action="/jobque/question/update" method="POST">
      <input type="hidden" name="board_id" value="<%= question.id %>">

      <label for="category">카테고리</label>
      <select name="category" id="category" required>
        <option value="기본" <%= question.category === '기본' ? 'selected' : '' %>>기본</option>
        <option value="인성" <%= question.category === '인성' ? 'selected' : '' %>>인성</option>
        <option value="프론트엔드" <%= question.category === '프론트엔드' ? 'selected' : '' %>>프론트엔드</option>
        <option value="백엔드" <%= question.category === '백엔드' ? 'selected' : '' %>>백엔드</option>
      </select>

      <label for="ques">질문</label>
      <textarea name="ques" id="ques" rows="5" required><%= question.ques %></textarea>

      <button type="submit">수정 완료</button>
    </form>

    <!-- 삭제 버튼 -->
    <form action="/jobque/question/delete/<%= question.id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');" style="margin-top: 20px;">
      <input type="hidden" name="board_id" value="<%= question.id %>">
      <button type="submit" class="delete-btn">질문 삭제</button>
    </form>

    <button onclick="history.back()">← 돌아가기</button>
  </main>
</body>
</html>
