<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>질문 수정</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/header', { user: session.userid }) %>

  <main class="form-wrapper">
    <div class="form-container">
      <h2 class="form-title">질문 수정</h2>

      <form action="/jobque/question/update" method="POST" class="question-form">
        <input type="hidden" name="board_id" value="<%= question.id %>">

        <div class="form-group">
          <label for="category">카테고리</label>
          <select name="category" id="category" required>
            <option value="기본" <%= question.category === '기본' ? 'selected' : '' %>>기본</option>
            <option value="인성" <%= question.category === '인성' ? 'selected' : '' %>>인성</option>
            <option value="프론트엔드" <%= question.category === '프론트엔드' ? 'selected' : '' %>>프론트엔드</option>
            <option value="백엔드" <%= question.category === '백엔드' ? 'selected' : '' %>>백엔드</option>
          </select>
        </div>

        <div class="form-group">
          <label for="ques">질문</label>
          <textarea name="ques" id="ques" rows="8" placeholder="질문 내용을 입력하세요" required><%= question.ques %></textarea>
        </div>

        <div class="answer-footer-buttons">
          <button type="button" class="btn btn-secondary" onclick="history.back()">돌아가기</button>
          <button type="submit" class="btn btn-primary">수정 완료</button>
        </div>
      </form>


    </div>
  </main>
</body>
</html>
