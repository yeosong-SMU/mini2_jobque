<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>답변 수정</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/header', { user: session.userid }) %>

  <main class="form-page">
    <h2 class="form-title">답변 수정</h2>

    <!-- 질문 내용 카드 -->
    <section class="question-preview">
      <p class="category">[<%= question.category %>]</p>
      <p class="ques"><%= question.ques %></p>
    </section>

    <!-- 기존 답변 카드 -->
    <section class="question-preview" style="background-color: #fffbe6;">
      <p style="font-weight: bold; margin-bottom: 6px;">내 기존 답변</p>
      <p class="ques"><%= comment.comment %></p>
    </section>

    <!-- 답변 수정 폼 -->
    <form action="/jobque/answer/update" method="POST" class="form-box">
      <input type="hidden" name="comment_id" value="<%= comment.id %>">
      <input type="hidden" name="board_id" value="<%= comment.board_id %>">

      <div class="form-group">
        <label for="comment">수정할 답변</label>
        <textarea name="comment" id="comment" rows="8" required><%= comment.comment %></textarea>
      </div>

      <div class="answer-footer-buttons">
        <button type="button" class="btn btn-secondary" onclick="history.back()">← 돌아가기</button>
        <button type="submit" class="btn btn-primary">수정 완료</button>
      </div>
    </form>

  </main>
</body>
</html>
