<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>회원가입</title>
  <link rel="stylesheet" href="/css/style.css">
  <script>
    function checkPasswordMatch() {
      const pwd = document.getElementById("pwd").value;
      const confirmPwd = document.getElementById("confirmPwd").value;
      const msg = document.getElementById("pwdMsg");

      if (confirmPwd === "") {
        msg.textContent = "";
        return;
      }

      if (pwd === confirmPwd) {
        msg.style.color = "green";
        msg.textContent = "비밀번호가 일치합니다.";
      } else {
        msg.style.color = "red";
        msg.textContent = "비밀번호가 일치하지 않습니다.";
      }
    }
  </script>
</head>
<body>
  <h1>회원가입</h1>

  <form action="/jobque/register" method="POST">
    <label for="name">이름</label><br>
    <input type="text" id="name" name="name" required><br><br>

    
    <label for="userid">아이디</label><br>
    <input type="text" id="userid" name="userid" required><br><br>

    <label for="pwd">비밀번호</label><br>
    <input type="password" id="pwd" name="pwd" required oninput="checkPasswordMatch()"><br><br>

    <label for="confirmPwd">비밀번호 확인</label><br>
    <input type="password" id="confirmPwd" oninput="checkPasswordMatch()" required><br>
    <span id="pwdMsg"></span><br><br>

    <button type="submit">가입하기</button>
  </form>

  <p>이미 계정이 있으신가요? <a href="/jobque/login">로그인</a></p>
</body>
</html>
